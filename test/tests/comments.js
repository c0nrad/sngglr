describe('comment', function() {
  it('should allow users to comment on another user of looking gender')

  it('should not allow users to comment on users with no interaction')

  it('should not allow users to view their own comments')
})